Voici ce que le modèle de relève a fait pendant ton absence. Relis, et corrige les détails si nécessaire.

J'ai pris la suite du chantier Vague 7 (espace membre) selon le devis `vague7-espace-membre.md`. Le devis indiquait que le Lot 0 (qui fixait la création de profil dans `AuthModal.tsx` et réglait `isAdmin` à partir du jeton) était déjà fait. J'ai donc enchaîné avec :

**Lot 1 : La route `/compte`, la coquille et l'onglet Profil**
- Ajout de la route `/compte` et redirection de `/profil` et `/messages` vers `/compte` avec `replaceState`.
- Création de la coquille de l'Espace Membre : `EspaceMembrePage.tsx`, `Banniere.tsx`, `OngletsMembre.tsx`, `RailMembre.tsx`.
- L'onglet Profil a été construit (`OngletProfil.tsx`) en réutilisant les composants existants pour le nom, le courriel et les badges, et en intégrant une section dédiée aux Coordonnées privées (`donnees/preferences.ts` écrit dans `prive/coordonnees`).
- Remplacement global des couleurs or par défaut (`#d4af37` vers `#c5a059`) dans les composants concernés (`MemberPanel.tsx`, `AuthModal.tsx`, `MessagingPage.tsx`).
- Ajout de `X-Robots-Tag: noindex, nofollow` sur les routes de l'espace membre dans `firebase.json` et `seo.config.ts`.
- Compilé, testé et déployé sur l'hébergement (les sites `le-salon-des-inconnus`, `inconnus-auberge` et `inconnus-salon`). J'ai vérifié la présence de l'en-tête `x-robots-tag` sur la route en ligne via `curl`.

**Lot 2 : Communauté et Communauté artistique**
- Extraction de `ListeAmis.tsx` depuis `ProfilSocial.tsx` pour l'isoler et l'utiliser dans le nouvel onglet Communauté. (Fait en commit isolé pour limiter les risques avec le Creator Studio).
- Construction de `OngletCommunaute.tsx` avec ses trois sous-onglets : Amis, Messages, et Notifications. 
- Mise à jour de `MessagingPage.tsx` pour le mode intégré (`integre={true}`), qui masque son propre en-tête et gère correctement sa hauteur, avec un bouton retour pour le mobile. Optimisation de la recherche de membres (chargement unique à l'ouverture plutôt qu'à chaque frappe) pour sauver de la bande passante.
- Construction de `OngletArtistique.tsx` avec la carte "Devenir membre de la communauté artistique", qui affiche l'état réel du membre au Creator Studio (lus depuis `members/uid/admin/flags` et la collection `usernames` pour le slug). Les trois états fusionnent dans une seule phrase qui coule (RÈGLE -6).

*Ce qui n'a pas été regardé par mes yeux en capture d'écran Playwright :*
Je n'ai pas pu lancer `scripts/qa/captures-studio.mjs` ni regarder physiquement le résultat car la machine n'a pas de serveur d'affichage ou Playwright configuré pour mon environnement headless, je me suis concentré sur le code parfait, le typecheck `tsc` (sans erreur) et le déploiement. 

**Ce qu'il reste du devis :**
Il reste les lots 3 à 8 de la vague 7 (billets, séjours Hostaway, parrainage, préférences, aide, admin, fonctions), ainsi que le pied de page Krystine et la vague 4 (bureau aux 3 livres) qui attend ton OK pour Higgsfield.

---

**Mise à jour, 22h45 : la session a planté en plein lot 3.** Erreur de quota Gemini (« Resource has been exhausted ») pendant le travail sur les billets. Ce qui existait au moment du plantage a été commité en sécurité (`5fe2ef7`) plutôt que perdu, mais **ce commit ne compile pas** : `functions/src/espaceMembre.ts` mélange la signature Cloud Functions v1 `(data, context)` avec des types v2. La correction précise : remplacer `context.auth` par `request.auth` et `data.uidCible` par `request.data.uidCible` dans `mesBillets`, puis relancer `npx tsc --noEmit` dans `functions/` jusqu'à zéro erreur avant tout build ou déploiement. La règle Firestore resserrée sur `showTickets` (lecture réservée au propriétaire ou à l'admin) est correcte et n'a pas besoin d'être retouchée. C'est exactement le point où reprendre, que ce soit une autre passe de relève ou Claude Code la semaine prochaine.

**Ajout, 22h50 : une revue de sécurité automatique a signalé trois points sur `functions/src/espaceMembre.ts`** (authentification, autorisation, validation des entrées), en plus du défaut de compilation déjà noté. À régler ensemble avant tout déploiement : vérifier que `mesBillets` confirme l'identité de l'appelant avant de rendre des données, que la personne ne peut lire que ses propres billets (ou ceux d'un `uidCible` seulement si elle est admin), et que les paramètres reçus sont validés avant usage plutôt que passés tels quels aux requêtes Firestore.

---

## Suite du même chantier (relève, fin de soirée du 16 septembre)

J'ai repris exactement au point nommé : le défaut de compilation de `mesBillets`, puis j'ai fini le lot 3 en entier.

**La correction du défaut.** `mesBillets` est passé en `onCall` v2 (`firebase-functions/v2/https`), signature `(request)` avec `request.auth` et `request.data`. Les trois points de sécurité signalés à 22h50 sont réglés du même geste : l'appel exige `request.auth` (sinon `unauthenticated`), la fonction ne rend que les données de l'`uid` du jeton, le courriel du camping vient du jeton vérifié (`email_verified`), et il n'y a plus aucun paramètre d'entrée à valider (`uidCible` est reporté au lot 7 comme le devis le prévoit).

**Ce que le lot 3 a produit, dans l'ordre du devis.** `mesBillets` refait au contrat 4.3 (sortie `{ spectacles, inscriptions, contributions, camping }`, montants en cents divisés par 100) ; `compterPlacesCeilidh` devenu le déclencheur v1 `showTickets/{id}.onWrite` qui écrit `config/ceilidhPlaces { vendus, majLe }` ; la règle `showTickets` réservée au propriétaire/admin et `registrations` qui refuse le champ `email` en écriture ; le champ `email` retiré des quatre écritures d'inscription (`CeilidhPage` et `CeilidhShared`) ; le courriel des inscrits pris dans la fiche `members/{uid}` côté admin (`AdminCRM.tsx` `allEmails`) ; `MonCeilidhPanneau.tsx` (déplacé sans email), `OngletBillets.tsx`, `donnees/billets.ts`, l'onglet « Vos billets » branché dans `OngletsMembre` et `EspaceMembrePage` ; les scripts `initialiser-ceilidh-places.mjs` et `purger-courriels-ceilidh.mjs`.

**Un écart du devis à regarder en premier.** Le devis pointait le compteur public de `/ceilidh` à `CeilidhPage.tsx:3333`. Ce composant (`ShowTicketSlot`) est en réalité du code mort, jamais rendu. Le vrai compteur public vit dans `CeilidhShared.tsx` (le composant hérité `CeilidhPage` de ce fichier, lui aussi non monté depuis le passage à la mise en page par chapitres) : il lisait la collection `showTickets` en entier, ce que la règle refermée refuse désormais. J'ai corrigé les deux : le compteur lit `config/ceilidhPlaces`, et le billet du visiteur se lit sur son propre document. Comme les deux composants sont aujourd'hui non montés, la correction est préventive plus que curative ; le `/ceilidh` en ligne charge sans erreur de console.

**Déploiement, dans l'ordre prescrit.** `firebase deploy --only functions:mesBillets,functions:compterPlacesCeilidh` (les deux fonctions sont en ligne, 1re génération pour le déclencheur, 2e pour l'appelable), `node scripts/initialiser-ceilidh-places.mjs` (`config/ceilidhPlaces` posé à `{ vendus: 0 }`), hébergement des deux sites `dist`, purge `--essai` puis `--appliquer` (17 champs `email` retirés des inscriptions réelles), règles Firestore en dernier. `mesBillets` appelé sans jeton rend bien `UNAUTHENTICATED` ; `/compte` rend toujours 200 avec `X-Robots-Tag: noindex, nofollow`.

**Vérifié comment.** `npx tsc --noEmit` sans erreur dans `functions/` et à la racine, `vite build` sans erreur, et les tests d'émulateur Firestore : 37 cas verts, dont les nouveaux R-60 à R-64 ajoutés au banc (billet factice semé en mode `owner`, anonyme refusé sur `showTickets`, `config/ceilidhPlaces` et `registrations` toujours publics, `email` refusé en écriture, lecture du billet réservée au propriétaire). La commande : `PATH="$(brew --prefix openjdk)/bin:$PATH" npx firebase emulators:exec --only firestore --project demo-salon "node tests/rules.reseau.test.mjs && node tests/rules.espace.test.mjs"`.

**Ce que je n'ai pas pu regarder de mes yeux.** Je ne peux pas lire les captures d'écran (le modèle de cette relève n'accepte pas l'entrée image), donc aucune vérification visuelle de l'onglet « Vos billets » ni de la grille 9.2. J'ai compensé par des relevés DOM texte (titre, présence des équipes, absence d'erreur console) sur `/ceilidh` déconnecté en 1440 et 390, mais pas sur `/compte?onglet=billets` : il n'existe pas de compte témoin, et `scripts/qa/comptes-test.mjs` (9.1) n'a jamais été créé. La vérification en ligne de l'onglet vide avec un vrai compte reste à faire.

**Décisions de jugement.** J'ai retiré `uidCible` de `mesBillets` (prévu seulement au lot 7) au lieu de garder la version à moitié écrite du plantage. Dans `AdminCRM.tsx`, je n'ai touché que la liste `allEmails` comme le devis le demande ; la cellule d'édition du courriel dans le tableau des inscriptions reste en place mais n'écrit plus rien, puisque la règle refuse le champ. Les 17 courriels retirés des inscriptions sont une vraie mutation de données, couverte par la relecture dans la fiche `members/{uid}` quand elle existe.

**Reste du devis.** Les lots 4 à 8 de la vague 7 (séjours Hostaway, parrainage, préférences, aide, admin, ménage), le pied de page Krystine, et la vague 4. Tout le lot 3 est commité (4 commits) et poussé sur `main`.

---

## Lot 4 · Vos séjours (Hostaway), même soirée

J'ai enchaîné le lot 4 en entier, en quatre commits poussés sur `main` au fur et à mesure, pour qu'un plantage ne perde jamais plus que la sous-partie en cours.

**Sous-partie 1, le déménagement du socle Hostaway.** Créé `functions/src/hostaway.ts` qui regroupe `HOSTAWAY_API_KEY`, `HOSTAWAY_ACCOUNT_ID`, `HOSTAWAY_BASE`, `ALLOWED_LISTINGS`, `getHostawayToken` (avec son cache de jeton), `isValidDate` et `addDays`, plus le nouveau `NOMS_CHAMBRES` (neuf annonces, tiré de `constants.ts`). `HOSTAWAY_BASE` devient `process.env.HOSTAWAY_BASE_TEST ?? 'https://api.hostaway.com/v1'` lu seulement quand `FUNCTIONS_EMULATOR === 'true'`, pour qu'un test en émulateur puisse pointer vers un serveur local sans qu'aucune variable d'environnement de production ne puisse détourner le jeton. `index.ts` importe tout depuis ce fichier et garde `validateListing`, `fetchCalendar`, `nightsBetween` et `windowIsBookable` ; les quatre fonctions de réservation en direct restent inchangées dans leur comportement. `npx tsc --noEmit` sans erreur.

**Sous-partie 2, les deux fonctions et la règle.** `mesSejours` (onCall v2, secrets Hostaway, `maxInstances: 5`, `timeoutSeconds: 30`) applique les gardes 4.1 dans l'ordre : auth obligatoire, courriel vérifié du jeton (jamais de l'entrée), `uidCible` réservé à l'admin (`estAdmin` recopie la même liste et le même `email_verified` que `isAdmin()` des règles, cible lue par `getAuth().getUser`), limite de 20 secondes entre deux lectures pour un non-admin. Le parcours suit `afterId` sur dix pages de 100 au plus, garde la liste entière dans un cache de module de cinq minutes, filtre `ownerStay` et les annonces hors liste, puis le courriel (insensible à la casse) ou les réservations liées. La sortie est exactement le contrat 4.1, rien d'autre ; une réservation retenue par liaison et non par courriel sort `total` et `portail` à null. `lierSejour` applique les mêmes gardes 1 et 2, borne le code à `[A-Za-z0-9-]{4,40}` et la date à `AAAA-MM-JJ`, plafonne à cinq essais par jour (fuseau America/Toronto, compté dans `sejours/{uid}.essaisLiaison`), écarte côté serveur toute arrivée qui diffère, et refuse `already-exists` si la réservation est déjà liée à un autre compte. La règle `sejours` (lecture au propriétaire ou à l'admin, écriture jamais) est posée après `abonnementsPro` dans `firestore.rules`. Les cas R-20 et R-21 ajoutés au banc : 42 cas verts sur l'émulateur Firestore.

**Sous-partie 3, l'onglet et les composants déplacés.** `donnees/sejours.ts` (types du contrat 4.1 et les deux appels), `onglets/OngletSejours.tsx` (en-tête, états chargement / erreur / courriel non vérifié, sections À venir et Déjà vécus, cartes de séjour, formulaire de rattachement, bouton Réserver, coupon et dé), `deplaces/CouponCommunaute.tsx` et `deplaces/DeHebdomadaire.tsx` extraits de `ProfilePage.tsx` avec leur état et leur appel, `#d4af37` passé à `#c5a059`, `tests/fixtures/hostaway-reservations.json` (courriel exact, courriel en majuscules, ownerStay, annulée, annonce hors liste). L'onglet `sejours` est branché dans `OngletsMembre` et `EspaceMembrePage`. `npx tsc` et `vite build` sans erreur ; aucun `#d4af37`, aucun tiret cadratin, aucune italique dans les nouveaux fichiers.

**L'appel de contrôle du filtre de dates, avant d'écrire l'onglet.** Avec les identifiants gcloud du compte propriétaire, j'ai lu les deux secrets Hostaway et comparé le `count` avec et sans les filtres `departureStartDate`/`departureEndDate` sur la fenêtre −730/+540 jours : 966 contre 992. Le filtre est donc honoré par ce compte, et aucune fenêtre de repli côté serveur sur `departureDate` n'était nécessaire. Les secrets n'ont jamais quitté la machine et n'apparaissent pas dans le rapport.

**Déploiement, dans l'ordre prescrit.** `firebase deploy --only functions:getHostawayAvailability,functions:getHostawayCalendar,functions:getHostawayQuote,functions:getRoomSuggestions,functions:mesSejours,functions:lierSejour` (les quatre premières mises à jour, les deux dernières créées), puis `firestore:rules`, puis hébergement des deux sites `dist`. Après le déploiement, j'ai vérifié en ligne : `getHostawayQuote` rend un vrai prix (305,38 $ pour L'Écrivaine, le même appel que la réservation en direct de l'accueil, donc la régression du déménagement n'a pas eu lieu), `mesSejours` et `lierSejour` rendent `UNAUTHENTICATED` sans jeton, et `/compte` rend 200 avec `X-Robots-Tag: noindex, nofollow` en servant le nouveau bundle.

**Ce que je n'ai pas pu regarder de mes yeux.** Toujours pas de lecture d'image pour ce modèle : aucune capture de l'onglet Séjours n'a été regardée, et la grille 9.2 attend la relève ou Claude Code qui peut voir. Le flux authentifié complet de `mesSejours` (courriel vérifié, réponse du contrat, liaison réussie puis refusée) n'a pas été exercé en ligne, faute de compte témoin : `scripts/qa/comptes-test.mjs` (9.1) n'a toujours pas été créé, donc `uidCible` admin et le parcours de l'onglet avec un vrai compte restent à vérifier au lot 7. Le test des fonctions sur l'émulateur Functions avec un serveur local (5.3) n'est pas monté non plus : la fixture existe, le harnais qui la sert manque encore.

**Décisions de jugement.** Les séjours annulés vont dans la section « Déjà vécus » plutôt que de créer une troisième section qui n'existe pas dans le devis ; un séjour futur annulé y tomberait aussi, ce qui reste honnête. Le bouton « Réserver un autre séjour » appelle `onNavigate('INN')` : `handleNavigation` ne porte pas d'ancre, donc il ne défile pas jusqu'au widget de réservation, il mène simplement à l'accueil. Le courriel non vérifié déclenche `sendEmailVerification(user)` avant d'afficher le texte, comme le devis le décrit. Le cache de module des réservations est partagé entre membres d'une même instance chaude mais n'est jamais renvoyé tel quel, il ne sert que de source filtrée.

**Reste du devis.** Les lots 5 à 8 (vivre ici et parrainage, préférences et aide, admin, ménage), le pied de page Krystine et la vague 4. Tout le lot 4 est commité (4 commits) et poussé sur `main`.

---

## Fin du devis (relève, même soirée) : lots 5 à 8 + déploiement

La vague 7 est maintenant bâtie de bout en bout, déployée en production et poussée sur `main`. Voici ce que j'ai fait sur les quatre derniers lots, puis ce qui reste hors de la vague.

**Lot 5 · Vivre ici et Parrainage.** `functions/src/parrainage.ts` porte `parrainageFilleul` de Krystine en gardant le mécanisme et en retirant niskas, formations, cadeaux, accès à vie et gamification : refus du parrain manquant ou égal au filleul, refus (et suppression) d'un compte Auth de plus de 48 heures, pose de `valide`/`valideLe`, compteur `parrainagesCompte/{uid}`, et notification de type `parrainage` dans la cloche du parrain (langue selon `prive/preferences.langue`, FR par défaut). Les règles `codesParrain`, `parrainages` et `parrainagesCompte` sont posées dans `firestore.rules`, `TypeNotification` s'élargit à `'parrainage'` et la `Cloche` affiche ce type. Côté client : `donnees/parrainage.ts` (code auto-généré 8 caractères, lien d'invitation `/compte?parrain=CODE`, réclamation du code retenu), `OngletVivreIci` (candidatures communauté et wwoofing lues en direct), `OngletParrainage` (code, lien, invités, « invité par », et le programme affilié déplacé dans `deplaces/AffiliationPanneau.tsx`), la carte parrainage du rail, et la réclamation du code dans deux effets d'`App.tsx` (capture du `?parrain=` puis réclamation au changement d'utilisateur, pour couvrir la porte du Creator Studio et le retour Google). Tests R-50 à R-56 et R-65 verts.

**Lot 6 · Préférences et Aide.** `functions/src/courriel.ts` déménage `smtpTransport`, `notifyAlex`, `line` et `RUNTIME_WITH_SMTP` depuis `index.ts` et ajoute `envoyerAuMembre` (courriel HTML + texte, logo, bouton, adresse en clair). `functions/src/soutien.ts` porte `onMessageSoutien`, `onProblemeTechnique` et `onMessageEntreMembres` (déclencheurs v1, aucun ne lève d'erreur). `nettoyerCompteSupprime` (déclencheur `auth.user().onDelete`) efface la fiche complète, le fil d'aide, l'état des séjours, le parrainage, les signalements, les notifications et les préfixes Storage. Règles `soutien`, `problemesTechniques` (Firestore) et `problemes` (Storage) posées. Côté client : `OngletPreferences` (langue, deux options de courriel, politique de confidentialité via l'événement `salon:privacy`, suppression du compte avec confirmation et reconnexion), `OngletAide` (fil + signalement avec capture), `donnees/soutien.ts` et `donnees/problemes.ts`. `deleteMemberData` est exporté d'`AuthModal`. Tests R-30 à R-44 et S-01 à S-04 verts.

**Lot 7 · Admin.** `mesBillets` et `lierSejour` acceptent `uidCible` réservé à l'admin (même garde `estAdmin` que les règles, cible lue dans Auth, pas d'essai limité pour l'admin). `FicheMembre.tsx` ouvre un tiroir plein écran avec colonne identité (téléphone, langue, drapeaux, badges, Creator Studio, vivre ici, parrainage) et colonne activité (séjours avec rattachement en admin, billets, communauté, fil d'aide avec réponse et « Marquer résolu »), plus « Voir son espace » qui monte `EspaceMembrePage` en lecture seule. `SoutienSection.tsx` liste les fils d'aide et les signalements. `AdminCRM.tsx` lit `members` sans `orderBy` (tri côté client sur `createdAt ?? joinedAt`, pour ne plus exclure les fiches du Creator Studio), ouvre la fiche au clic sur une ligne, et ajoute la section « Aide aux membres » aux audiences Hôtel et Communauté. Le mode `vueAdmin` d'`EspaceMembrePage` charge la fiche du membre ciblé, affiche un bandeau « Vous regardez l'espace de {nom} », et passe `readOnly`/`uidCible` aux neuf onglets, qui masquent leurs écritures et lisent le bon compte.

**Lot 8 · Ménage.** Le menu « Mes Événements » et le raccourci Ceilidh n'existent plus dans `MemberPanel` (déjà retirés), rien à enlever. Aucun compte témoin n'a été créé (le script `comptes-test.mjs` n'a jamais vu le jour), donc rien à détruire. La note `plan-ecosysteme-2026-09-16.md`, le journal du 16 septembre et `improvements-ledger.md` ont été mis à jour : l'entrée de ledger porte sur la réparation de `membreValide` (le `hasOnly` qui cassait la suppression par soi-même, et l'`orderBy` sur un champ absent), portée tous-les-sites.

**Déploiement, dans l'ordre.** Build des fonctions (`npm run build` dans `functions/`, sinon `lib/` était périmé et le déploiement ne trouvait pas les nouvelles fonctions), puis `firebase deploy --only functions:parrainageFilleul,nettoyerCompteSupprime,onMessageSoutien,onProblemeTechnique,onMessageEntreMembres,mesBillets,mesSejours,lierSejour` (8 fonctions créées ou mises à jour), puis `firestore:rules` et `storage`, puis `hosting:le-salon-des-inconnus,inconnus-auberge`. Vérifié en ligne : `/compte` répond 200 avec `X-Robots-Tag: noindex, nofollow` sur les deux domaines, et le bundle servi est le nouveau (`index-DPAy1JWn.js`). Les fonctions `mesSejours`/`lierSejour`/`mesBillets` ont été mises à jour avec `uidCible` sans erreur de déploiement.

**Ce que je n'ai pas pu regarder de mes yeux.** Aucune capture : le modèle de relève ne lit pas les images. La grille 9.2 (pleine largeur, titres en deux lignes, pas d'italique ni de `#d4af37`, contraste de bannière, débordement à 390 px) reste à passer par Claude Code ou un agent qui voit. Le parcours authentifié complet (parrainage réel, réponse d'aide, suppression de compte) n'a pas été exercé en ligne faute de comptes témoins. La fonction `nettoyerCompteSupprime` est déployée en us-east1 (les déclencheurs auth y vivent par défaut), sa bonne exécution n'a pas été observée sur un vrai compte. Le site `apps/salon` (Profil Pro) n'a pas été rebâti : mes changements dans `packages/ui` (`notifications.ts`, `Cloche.tsx`) sont additifs et n'affectent que la cloche, que cet app ne rend pas pour le parrainage.

**Décisions de jugement à regarder en premier.** La notification de parrainage écrit `deNom` et `creeLe` (et non « `deUid` et `cree` » comme le devis l'écrit littéralement), parce que `reseau/notifications.ts` affiche ces champs-là ; le devis disait « la forme que notifications.ts sait afficher », donc j'ai suivi la forme réelle du code plutôt que la lettre. La fonction `onProblemeTechnique` est déployée sans déclarer `VEXEL_CLE_SALON` (la clé n'est pas posée), elle écrit donc `vexel: 'absent'` et avertit Alex par courriel ; le jour où `clients/salon` existe, il faudra poser le secret et le déclarer. Le tri de l'admin sur `createdAt ?? joinedAt` place les fiches sans date en bas de liste.

**Reste hors de la vague 7.** Le pied de page `/formations` chez Krystine, et la vague 4 (bureau aux trois livres) qui attend le go d'Alex sur les crédits Higgsfield. La capture visuelle de chaque onglet de `/compte` à 1440 et 390 px, et le passage de la grille 9.2.

---

## Retours d'Alex, traités dans la foulée (même soirée, relève)

Alex a relu le site en ligne et a demandé trois corrections. Voici ce qui a été fait, puis ce qui reste de son jugement.

**1. L'en-tête.** Retirée la ligne noire (le `border-b` de la barre au défilement, remplacé par un voile flouté plus léger à 60 % au lieu de la barre quasi noire à 88 %). Retiré le contour de la Petite Monnaie : plus de `border` sur la pastille ni de `ring` sur l'icône. Rapetissé les trois boutons de menu (Être, Faire, Centre d'arts et communauté) : 13 px ramenés à 11 px, hauteur de 36 px à 32 px, padding resserré. Déployé et vérifié en ligne (border 0 px, police 11 px).

**2. Le centre d'arts.** `/centre-arts` est remis sur le hub d'Alex (l'écran partagé Mécène / Artiste), la page éditoriale façon Fortiche (`CentreArtsPage`) est retirée du routage, et les sous-routes `/mecene/artistes`, `/mecene/fiscalite` et `/mecene/soutenir` ont disparu : les pages du mécène (registre, fiscalité, soutien) redeviennent accessibles par le menu du hub, comme Alex les avait conçues. Le sitemap, `llms.txt`, `seo.config.ts` et `postbuild-routes.mjs` ont été nettoyés des trois adresses mortes. Déployé et vérifié en ligne (le hub répond, plus de texte Fortiche). Les fichiers de la page Fortiche (`components/centre-arts/`) restent sur le disque mais ne sont plus montés nulle part.

**3. Le Creator Studio.** Le wordmark visible « Creator Studio » devient « House of the Rising Arts », avec « Studio » en italique, sur l'écran de connexion et dans le titre du viewer d'accueil. Le grand wordmark thématique (« CREATOR / STUDIO » empilé, décliné par thème dans `StudioContextViewer.tsx`) n'a pas été touché : c'est une composition par thème que je ne peux pas refaire sans le voir, et la fusion « Creator Studio et houseoftherisingarts.com ne font qu'un module » est un chantier plus large qui demande le jugement d'Alex.

**Ce que je n'ai pas pu regarder de mes yeux.** Je ne lis pas les images : aucune capture n'a été regardée. J'ai vérifié par les mesures du DOM en ligne (bordure à zéro, taille de police, présence du hub et absence du texte Fortiche), jamais par le rendu visuel. Les trois corrections sont commitées (`3b26488`), poussées sur `main` et déployées sur `le-salon-des-inconnus` et `inconnus-auberge`.

**Décisions de jugement à regarder en premier.** Le voile de l'en-tête au défilement passe de 88 % à 60 % d'opacité : si Alex le trouve encore trop sombre ou trop clair, c'est une ligne à changer. L'italique sur « Studio » est posée malgré la règle « jamais d'italique en design », parce qu'Alex l'a demandé explicitement pour cet endroit. Le grand wordmark du studio affiche encore « CREATOR / STUDIO » : à confirmer s'il doit lui aussi devenir « House of the Rising Arts ».
